TERRITORY (not a finished joke — find the specific thing inside it):
{topic}

FORM (mandatory): {form}
ENGINE (mandatory): {engine}
FORBIDDEN_ENGINE (used on the last bit): {last_engine}
SPOKEN SHAPE (mandatory): {shape}
LAST SPOKEN SHAPE (do not repeat): {last_shape}
FEW-SHOT SHAPE (mandatory — name this turn before you write): {few_shot_shape}
LAST FEW-SHOT SHAPE (do not repeat): {last_few_shot_shape}
ALREADY USED TONIGHT — do not reuse any of these, in any form: {avoid}

Listener: {user_context}
Style hint: {style}

{room_feedback}

Find the smallest, most specific thing in that territory — the one person,
object, brand, or number nobody else would have picked — and build the bit on
that, using the named few-shot shape. Start inside the bit. Punch on the last
word. Then output:

FINAL: <the line>
